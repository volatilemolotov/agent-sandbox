apiVersion: extensions.agents.x-k8s.io/v1beta1
kind: SandboxWarmPool
metadata:
  name: simple-sandbox-warmpool
  namespace: gymnasium
spec:
  replicas: 2
  sandboxTemplateRef:
    name: simple-sandbox-template
